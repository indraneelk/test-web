name = "mmw-tm"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "public"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "task-manager-db-v2"
database_id = "6e646fd2-4e3c-4632-b7e7-55e224368b5f"

# Environment variables (non-sensitive)
[vars]
ENVIRONMENT = "production"
SUPABASE_URL = "https://oxbaswpyxryvygamgtsu.supabase.co"

# Secrets (add these with: wrangler secret put <NAME>)
# For Pages, use: wrangler pages secret put <NAME> --project-name=mmw-tm
# Required secrets:
# wrangler pages secret put SUPABASE_ANON_KEY --project-name=mmw-tm
# wrangler pages secret put SUPABASE_SERVICE_ROLE_KEY --project-name=mmw-tm
# wrangler pages secret put SUPABASE_JWT_SECRET --project-name=mmw-tm
# wrangler pages secret put SESSION_SECRET --project-name=mmw-tm
# wrangler pages secret put DISCORD_BOT_SECRET --project-name=mmw-tm
# Optional secrets:
# wrangler pages secret put DISCORD_PUBLIC_KEY --project-name=mmw-tm (for Discord OAuth)
# wrangler pages secret put ANTHROPIC_API_KEY --project-name=mmw-tm (for Claude AI features)

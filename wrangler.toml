name = "mmw-tm"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "public"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "task-manager-db-v2"
database_id = "6e646fd2-4e3c-4632-b7e7-55e224368b5f"

# Environment variables (non-sensitive)
[vars]
ENVIRONMENT = "production"
SUPABASE_URL = "https://oxbaswpyxryvygamgtsu.supabase.co"

# Secrets (add these with: wrangler secret put <NAME>)
# For Pages, use: wrangler pages secret put <NAME> --project-name=mmw-tm
# wrangler pages secret put SUPABASE_ANON_KEY
# wrangler pages secret put SUPABASE_SERVICE_ROLE_KEY
# wrangler pages secret put SUPABASE_JWT_SECRET
# wrangler pages secret put SESSION_SECRET
# wrangler pages secret put ANTHROPIC_API_KEY (optional, for Claude AI features)

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Session Secret (REQUIRED for production)
# Generate a strong random secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-secret-key-here

# Claude AI Integration (REQUIRED for AI features)
# Get your API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Discord Bot Integration (Optional)
# Create a bot at: https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your-discord-bot-token-here
API_BASE_URL=http://localhost:3000/api

# Cloudflare D1 Database (Optional - for production)
# Get these from: https://dash.cloudflare.com
# 1. Account ID: Dashboard → Domain → Overview (right sidebar)
# 2. Create D1 database: wrangler d1 create task-manager
# 3. Create API token: Dashboard → Profile → API Tokens → Create Token
CLOUDFLARE_ACCOUNT_ID=your-account-id-here
CLOUDFLARE_D1_DATABASE_ID=your-database-id-here
CLOUDFLARE_API_TOKEN=your-api-token-here

# Supabase Authentication (Optional - for magic link auth)
# Get these from: https://app.supabase.com/project/_/settings/api
# 1. Create a project at https://app.supabase.com
# 2. Go to Project Settings → API
# 3. Copy the URL and anon/public key
SUPABASE_URL=your-supabase-url-here
SUPABASE_ANON_KEY=your-supabase-anon-key-here
# Service role key (server-side only, keep secret!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
